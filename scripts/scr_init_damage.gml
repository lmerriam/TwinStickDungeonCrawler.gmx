///scr_init_damage(damage_inst,creator,damage,knockback)
var inst = argument[0];
var creator = argument[1];
var damage = argument[2];
var knockback = argument[3];

inst.creator = creator;
inst.damage = damage;
inst.knockback = knockback;
