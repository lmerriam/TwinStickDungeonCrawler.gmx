///scr_init_enemy_stats
base_xp = starting_damage + ((level-1) * (base_xp/10));
xp = base_xp;

base_damage = starting_damage + ((level-1) * (base_damage/10));
damage = base_damage;

base_hp = starting_hp + ((level-1) * (base_hp/10));
hp = base_hp;
